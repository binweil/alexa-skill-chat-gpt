{
  "name": "root",
  "private": true,
  "scripts": {
    "release": "./aws-lambda-helper.sh && lerna clean --yes && lerna bootstrap && lerna run release",
    "deploy": "npm run release && lerna run deploy"
  },
  "devDependencies": {
    "lerna": "^6.6.2"
  }
}
